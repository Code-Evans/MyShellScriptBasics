# Standard Input, Output, and Error Redirection

## 📌 Overview

In shell scripting, **Standard Input (stdin)**, **Standard Output (stdout)**, and **Standard Error (stderr)** can be redirected **from or to files** using redirection operators such as `<`, `>`, and `2>`.

Redirection is commonly used for:
- Logging
- Debugging
- Automation
- Separating normal output from error messages

---

## 📥 Standard Input (stdin)

Standard Input allows commands or programs to **read input from a file** instead of the keyboard.

### Operator
- `<`

### Example
```ksh
cat < README.txt
```

---

## 📤 Standard Output (stdout)

Standard Output is the normal output produced by a command.  
It can be redirected to a file using:

### Operators
- `>`
- `1>`

### Example
```ksh
echo "Hello World!" > process.log
```

---

## ❌ Standard Error (stderr)

Standard Error is used for error messages generated by commands.

### Operator
- `2>`

### Example
```ksh
cd /this/does/not/exist 2> process.log
```

---

## ➕ Appending Output and Error

To **append** output or error messages to a file instead of overwriting it:

### Operators
- `>>`  → append stdout
- `2>>` → append stderr

### Example
```ksh
echo "New entry" >> process.log
cd /invalid/path 2>> process.log
```

---

## 🔀 Redirecting Both Output and Error

You can redirect **both stdout and stderr** to the same file.

### Examples

```ksh
cat this_does_not_exist.txt > process.log 2>&1
cat this_does_not_exist.txt >> process.log 2>&1
cat this_does_not_exist.txt 2> process.log >&2
cat this_does_not_exist.txt 2>> process.log >&2
```

---

## 📝 Notes

- File descriptor numbers:
  - `0` → Standard Input
  - `1` → Standard Output
  - `2` → Standard Error
- Order of redirection **matters**
- Redirection is handled by the shell, not the command itself
